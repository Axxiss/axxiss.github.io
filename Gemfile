source 'https://rubygems.org'

gem 'jekyll', '~> 4.4'

group :jekyll_plugins do
  gem 'jekyll-paginate', '~> 1.1'
end

# Not part of the site: the link check in bin/check-links.
group :development do
  gem 'html-proofer', '~> 5.2'
end
